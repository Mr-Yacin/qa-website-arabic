# Neon Database (for storing contact messages and ratings)
DATABASE_URL=postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/dbname?sslmode=require

# Resend Email Service (for contact form notifications)
RESEND_API_KEY=your_resend_api_key

# Admin Email Configuration
ADMIN_EMAIL=admin@yourdomain.com
FROM_EMAIL=noreply@yourdomain.com

# Optional: Deployment flags (automatically set by hosting platforms)
VERCEL=1
NODE_ENV=production